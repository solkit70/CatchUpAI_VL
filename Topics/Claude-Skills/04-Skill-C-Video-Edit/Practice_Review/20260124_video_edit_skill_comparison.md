# AI4PKM vs CatchUpAI Video Edit Skill 비교 분석

**작성일**: 2026-01-24
**작성자**: Claude
**목적**: 두 영상 편집 자동화 스킬의 기능, 워크플로우, 성능 비교

---

## 1. 개요

두 스킬 모두 영상에서 **무음 구간**과 **필러 단어**를 자동으로 제거하는 기능을 제공합니다. 하지만 **무음을 감지하는 방식**과 **처리 아키텍처**가 근본적으로 다릅니다.

| 구분 | AI4PKM Video Cleaning | CatchUpAI Video Edit |
|------|----------------------|---------------------|
| **핵심 방식** | 단어 타임스탬프 기반 | 음량(데시벨) 기반 |
| **API 의존성** | OpenAI Whisper API (유료) | 로컬 처리 (무료) |
| **스크립트 구성** | 2단계 (transcribe → edit) | 1단계 (통합) |
| **편집 철학** | 보수적 (자연스러운 결과) | 공격적 (최대 단축) |

---

## 2. 워크플로우 비교

### 2.1 AI4PKM Video Cleaning Skill

```
┌─────────────────────────────────────────────────────────────────┐
│  AI4PKM Video Cleaning Workflow                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [Step 1: transcribe_video.py]                                  │
│  ┌─────────┐    ┌──────────┐    ┌─────────────────┐            │
│  │ 영상    │ -> │ 오디오   │ -> │ OpenAI Whisper  │            │
│  │ 파일    │    │ 추출     │    │ API 호출        │            │
│  └─────────┘    └──────────┘    └────────┬────────┘            │
│                                          │                      │
│                                          v                      │
│                              ┌───────────────────────┐          │
│                              │ transcript.json       │          │
│                              │ (단어별 시작/종료 시간)│          │
│                              └───────────┬───────────┘          │
│                                          │                      │
│  [Step 2: edit_video_remove_pauses.py]   │                      │
│                                          v                      │
│                              ┌───────────────────────┐          │
│                              │ 단어 사이 간격 분석   │          │
│                              │ (pause > threshold)   │          │
│                              └───────────┬───────────┘          │
│                                          │                      │
│                                          v                      │
│                              ┌───────────────────────┐          │
│                              │ FFmpeg 세그먼트 편집  │          │
│                              │ (유지 구간만 추출)    │          │
│                              └───────────┬───────────┘          │
│                                          │                      │
│                                          v                      │
│                              ┌───────────────────────┐          │
│                              │ 편집된 영상 출력      │          │
│                              └───────────────────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

**핵심 로직**:
```python
# 단어 사이 간격으로 무음 감지
for i in range(len(words) - 1):
    current_end = words[i]['end']      # 현재 단어 종료 시간
    next_start = words[i + 1]['start'] # 다음 단어 시작 시간
    pause_duration = next_start - current_end

    if pause_duration >= threshold:    # 예: 1.0초 이상
        pauses.append((current_end, next_start, pause_duration))
```

### 2.2 CatchUpAI Video Edit Skill

```
┌─────────────────────────────────────────────────────────────────┐
│  CatchUpAI Video Edit Workflow                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [video-editor.py - 통합 스크립트]                              │
│                                                                 │
│  ┌─────────┐    ┌──────────────────────┐                       │
│  │ 영상    │ -> │ pydub로 오디오 로드  │                       │
│  │ 파일    │    │ (AudioSegment)       │                       │
│  └─────────┘    └──────────┬───────────┘                       │
│                            │                                    │
│                            v                                    │
│               ┌────────────────────────┐                       │
│               │ detect_silence()       │                       │
│               │ - 임계값: -40dB        │                       │
│               │ - 최소 길이: 500~1000ms│                       │
│               └────────────┬───────────┘                       │
│                            │                                    │
│                            v                                    │
│               ┌────────────────────────┐                       │
│               │ 무음 구간 리스트 생성  │                       │
│               │ [(start_ms, end_ms)]   │                       │
│               └────────────┬───────────┘                       │
│                            │                                    │
│                            v                                    │
│               ┌────────────────────────┐                       │
│               │ FFmpeg select 필터     │                       │
│               │ (100개씩 묶음 처리)    │                       │
│               └────────────┬───────────┘                       │
│                            │                                    │
│                            v                                    │
│               ┌────────────────────────┐                       │
│               │ 묶음 병합 → 최종 출력  │                       │
│               └────────────────────────┘                       │
└─────────────────────────────────────────────────────────────────┘
```

**핵심 로직**:
```python
# 음량 기반 무음 감지 (pydub)
silence_ranges = detect_silence(
    audio,
    min_silence_len=min_silence_ms,  # 예: 1000ms
    silence_thresh=threshold_db      # 예: -40dB
)
# 결과: 음량이 -40dB 이하이고 1초 이상 지속되는 모든 구간
```

---

## 3. 무음 감지 방식 비교

### 3.1 AI4PKM: 단어 타임스탬프 기반

**원리**: OpenAI Whisper API가 음성을 인식하여 각 단어의 시작/종료 시간을 반환합니다. 이 메타데이터를 분석하여 **단어와 단어 사이의 간격**이 threshold 이상인 구간을 "무음"으로 판단합니다.

**특징**:
- 말하는 중간의 자연스러운 쉼(호흡, 생각)은 유지됨
- "단어가 없는 구간"만 무음으로 인식
- 음성이 있지만 인식되지 않은 경우 (웅얼거림 등)는 무음으로 처리 안 됨

**예시**:
```
"안녕하세요" (0.0s ~ 1.2s) ... 3초 침묵 ... "반갑습니다" (4.2s ~ 5.5s)
                               ↑
                    이 구간만 무음으로 감지 (단어 사이 간격 3초)
```

### 3.2 CatchUpAI: 음량(데시벨) 기반

**원리**: pydub 라이브러리로 오디오의 음량을 분석하여 **특정 데시벨 이하**인 구간을 "무음"으로 판단합니다.

**특징**:
- 소리가 작은 모든 구간을 감지 (말 사이의 짧은 쉼도 포함)
- 배경 소음이 있으면 무음 감지가 어려울 수 있음
- 말하는 중간에 잠깐 멈추는 부분도 무음으로 감지 가능

**예시**:
```
"안녕" (0.0s ~ 0.5s) [0.3초 쉼] "하세요" (0.8s ~ 1.2s) ... 3초 침묵 ...
                      ↑                                      ↑
              이 구간도 감지 가능 (음량 -40dB 이하)    이 구간도 감지
```

### 3.3 감지 방식 차이의 결과

| 상황 | AI4PKM | CatchUpAI |
|------|--------|-----------|
| 말 사이의 긴 침묵 (3초) | 감지 O | 감지 O |
| 말 중간의 짧은 쉼 (0.5초) | 감지 X | 감지 O (설정 따라) |
| 낮은 목소리로 말하는 중 | 감지 X | 감지할 수 있음 |
| "음..." 하며 생각하는 중 | 감지 X (단어로 인식) | 감지할 수 있음 |
| 배경 음악이 있는 침묵 | 감지 O | 감지 X |

---

## 4. 기술 스택 비교

### 4.1 AI4PKM Video Cleaning

| 구성 요소 | 기술 | 역할 |
|----------|------|------|
| 음성 인식 | OpenAI Whisper API | 단어별 타임스탬프 추출 |
| 오디오 추출 | FFmpeg | 영상에서 오디오 분리 |
| 영상 편집 | FFmpeg (segment cut) | 구간별 영상 추출 및 병합 |
| 스크립트 언어 | Python 3.7+ | openai 패키지 사용 |

**의존성**:
```bash
pip install openai       # OpenAI API 클라이언트
# FFmpeg 필수 (시스템 설치)
```

### 4.2 CatchUpAI Video Edit

| 구성 요소 | 기술 | 역할 |
|----------|------|------|
| 오디오 분석 | pydub | 음량 기반 무음 감지 |
| 필러 감지 (선택) | Local Whisper | 필러 단어 음성 인식 |
| 영상 편집 | FFmpeg (select filter) | between(t,start,end) 표현식 |
| 스크립트 언어 | Python 3.7+ | 로컬 처리 |

**의존성**:
```bash
pip install pydub           # 오디오 분석
pip install openai-whisper  # 필러 감지용 (선택)
# FFmpeg 필수 (시스템 설치)
```

---

## 5. FFmpeg 편집 방식 비교

### 5.1 AI4PKM: 세그먼트 분할 후 병합

```bash
# 각 세그먼트를 개별 파일로 추출
ffmpeg -ss 0.0 -i input.mkv -t 5.0 segment_0001.mp4
ffmpeg -ss 8.0 -i input.mkv -t 3.0 segment_0002.mp4
...

# concat demuxer로 병합
ffmpeg -f concat -safe 0 -i concat_list.txt -c copy output.mkv
```

**특징**:
- 각 세그먼트마다 FFmpeg 호출
- 세그먼트 수가 많으면 오버헤드 증가
- drawtext 필터로 "Skipping X secs" 텍스트 오버레이 가능

### 5.2 CatchUpAI: select 필터 사용

```bash
# select 필터로 유지할 구간만 선택
ffmpeg -i input.mkv \
  -filter_complex "[0:v]select='between(t,0,5)+between(t,8,11)+...',setpts=N/FRAME_RATE/TB[v]; \
                   [0:a]aselect='between(t,0,5)+between(t,8,11)+...',asetpts=N/SR/TB[a]" \
  -map "[v]" -map "[a]" output.mkv
```

**특징**:
- 100개 구간씩 묶음 처리 (명령어 길이 제한 대응)
- 한 번의 FFmpeg 호출로 여러 구간 처리
- 긴 영상에서 효율적 (이전 concat 필터 대비 개선)

---

## 6. 실제 테스트 결과 비교 (2026-01-24)

### 6.1 테스트 환경

- **입력 파일**: original.mkv (92.2분, 약 364MB)
- **무음 기준**: 1.0초 이상 (두 스킬 동일하게 설정)
- **필러 단어**: 제거 안 함 (무음 구간만 테스트)

### 6.2 결과 비교

| 항목 | AI4PKM | CatchUpAI |
|------|--------|-----------|
| **원본 길이** | 92.2분 | 92.2분 |
| **편집 후 길이** | 81.0분 | 68.9분 |
| **단축 비율** | **12.1%** | **25.3%** |
| **감지된 무음** | 266개 | 761개 |
| **처리 시간** | 50분 | 1시간 27분 |
| **API 비용** | ~$0.55 | 무료 |
| **파일 크기 제한** | 25MB (API) | 없음 |

### 6.3 결과 분석

```
┌─────────────────────────────────────────────────────────────┐
│ 동일한 1초 threshold에서 감지된 무음 구간 수                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  AI4PKM:    ████████████████████████████ 266개 (12.1% 단축) │
│                                                             │
│  CatchUpAI: ████████████████████████████████████████████████│
│             ████████████████████████████████████████████████│
│             ████████████████████████████████ 761개 (25.3%)  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**왜 이런 차이가 발생하는가?**

1. **CatchUpAI가 더 많은 무음을 감지하는 이유**:
   - 말하는 중간의 짧은 쉼표도 음량이 낮으면 무음으로 감지
   - "단어" 개념이 없으므로 소리가 작은 모든 구간을 포착
   - 웅얼거림, 낮은 목소리도 -40dB 이하면 무음 처리

2. **AI4PKM이 더 보수적인 이유**:
   - Whisper가 인식한 "단어" 사이의 간격만 무음으로 판단
   - 말하는 도중의 자연스러운 쉼은 단어에 포함됨
   - 음성이 인식되면 음량과 관계없이 유지

---

## 7. 장점과 단점

### 7.1 AI4PKM Video Cleaning

#### 장점
| 장점 | 설명 |
|------|------|
| **자연스러운 결과** | 단어 기반이라 말의 흐름이 자연스럽게 유지됨 |
| **필러 단어 정밀 제거** | "어", "음" 등 특정 단어만 정확히 제거 가능 |
| **빠른 처리** | API 호출이므로 로컬 GPU 없이도 빠름 |
| **다국어 지원** | Whisper API의 강력한 다국어 인식 |
| **Transcript 부산물** | 자막 파일(.json, .md)도 함께 생성 |

#### 단점
| 단점 | 설명 |
|------|------|
| **유료 (API 비용)** | $0.006/분, 1시간 영상 약 $0.36 |
| **파일 크기 제한 (과거 버전)** | 과거 버전은 25MB API 제한으로 긴 영상을 수동 분할해야 했음. **v3.0에서 자동 청킹 기능으로 해결됨.** |
| **인터넷 필요** | 오프라인에서 사용 불가 |
| **데이터 외부 전송** | 민감한 콘텐츠는 주의 필요 |
| **보수적 편집** | 단축률이 낮아 효과가 덜 느껴질 수 있음 |

### 7.2 CatchUpAI Video Edit

#### 장점
| 장점 | 설명 |
|------|------|
| **완전 무료** | API 비용 없음 |
| **파일 크기 무제한** | 로컬 처리라 제한 없음 |
| **오프라인 사용** | 인터넷 연결 불필요 |
| **공격적 편집** | 단축률이 높아 효과가 확실함 |
| **데이터 보안** | 로컬에서만 처리, 외부 전송 없음 |
| **세밀한 조정** | threshold, min-silence 등 파라미터 조정 가능 |

#### 단점
| 단점 | 설명 |
|------|------|
| **부자연스러울 수 있음** | 말 중간의 쉼도 제거되어 급한 느낌 |
| **긴 처리 시간** | 92분 영상 처리에 1시간 27분 소요 |
| **배경 소음에 취약** | 소음이 있으면 무음 감지 정확도 저하 |
| **필러 감지 제한** | 로컬 Whisper 필요, GPU 권장 |
| **단순 음량 기반** | 낮은 목소리가 잘려나갈 수 있음 |

---

## 8. 사용 시나리오별 권장 스킬

### 8.1 AI4PKM 권장 상황

| 시나리오 | 이유 |
|---------|------|
| **인터뷰 영상** | 자연스러운 대화 흐름 유지 중요 |
| **강의/프레젠테이션** | 설명 중간의 쉼이 이해에 도움 |
| **팟캐스트** | 청취 경험의 자연스러움이 중요 |
| **한국어 필러 제거** | "어", "음" 등 정확한 제거 필요 |
| **자막이 필요한 경우** | Transcript 파일 자동 생성 |

### 8.2 CatchUpAI 권장 상황

| 시나리오 | 이유 |
|---------|------|
| **개인 YouTube 영상** | 최대한 짧게, 비용 무료 |
| **긴 회의 녹화** | 파일 크기 제한 없음 |
| **민감한 콘텐츠** | 외부 전송 없이 로컬 처리 |
| **오프라인 환경** | 인터넷 없이 사용 |
| **빠른 결과 원할 때** | 높은 단축률 (25%+) |
| **비용이 제약인 경우** | 완전 무료 |

---

## 9. 파라미터 가이드

### 9.1 AI4PKM 파라미터

```bash
python edit_video_remove_pauses.py video.mp4 \
  --pause-threshold 1.0 \  # 단어 사이 간격 (초)
  --padding 0.1 \          # 컷 주변 패딩 (초)
  --no-fillers             # 필러 제거 비활성화
```

| 파라미터 | 기본값 | 설명 |
|---------|-------|------|
| `--pause-threshold` | 1.0 | 무음으로 판단할 최소 단어 간격 (초) |
| `--padding` | 0.1 | 컷 앞뒤에 추가할 여유 시간 |
| `--no-fillers` | false | 필러 단어 제거 비활성화 |
| `--preview` | false | 편집 없이 결과 미리보기 |

### 9.2 CatchUpAI 파라미터

```bash
python video-editor.py video.mp4 \
  --remove-silence \
  --threshold -40 \        # 무음 판단 데시벨
  --min-silence 1000       # 최소 무음 길이 (ms)
```

| 파라미터 | 기본값 | 설명 |
|---------|-------|------|
| `--threshold` | -40 | 무음 판단 임계값 (dB) |
| `--min-silence` | 500 | 무음으로 판단할 최소 길이 (ms) |
| `--remove-fillers` | false | 필러 단어 제거 활성화 |
| `--model` | base | Whisper 모델 (필러 감지용) |

### 9.3 동일한 결과를 얻으려면?

두 스킬에서 유사한 결과를 얻기 위한 파라미터 매핑:

```
AI4PKM --pause-threshold 1.0  ≈  CatchUpAI --min-silence 1000

단, 감지 방식이 다르므로 결과는 동일하지 않음!
- AI4PKM: 단어 사이 1초 간격 → 266개 감지
- CatchUpAI: 음량 1초 이하 → 761개 감지
```

---

## 10. 결론 및 권장 사항

### 10.1 요약

| 기준 | 승자 | 설명 |
|------|------|------|
| **비용** | CatchUpAI | 완전 무료 vs $0.55/92분 |
| **처리 속도** | AI4PKM | 50분 vs 1시간 27분 |
| **단축률** | CatchUpAI | 25.3% vs 12.1% |
| **자연스러움** | AI4PKM | 단어 기반 보수적 편집 |
| **파일 크기** | CatchUpAI | 제한 없음 vs 25MB (v3.0 이전) |
| **오프라인** | CatchUpAI | 로컬 처리 가능 |
| **다국어** | AI4PKM | Whisper API 다국어 지원 |

### 10.2 최종 권장

1. **자연스러운 결과 + 자막 필요** → **AI4PKM**
2. **최대 단축 + 비용 절감** → **CatchUpAI**
3. **긴 영상 (2시간+)** → **AI4PKM (v3.0+) 또는 CatchUpAI 모두 사용 가능**
4. **민감한 콘텐츠** → **CatchUpAI** (로컬 처리)

### 10.3 하이브리드 접근법

두 스킬의 장점을 결합할 수 있습니다:

1. **1차: CatchUpAI로 공격적 편집** (무료, 빠른 단축)
2. **2차: AI4PKM으로 미세 조정** (필러 단어 정밀 제거)

또는:

1. **짧은 영상 (< 30분)**: AI4PKM (자연스러운 결과)
2. **긴 영상 (> 30분)**: CatchUpAI (비용/시간 효율)

---

## 부록: 테스트 데이터

### A. AI4PKM 테스트 결과 (2026-01-24 오전)

```
입력: original.mkv (92.2분, 364MB)
출력: original_edited_AI4PKM.mkv (81.0분, 318MB)
단축: 11.2분 (12.1%)
무음: 266개 감지
비용: ~$0.55
시간: 50분
```

### B. CatchUpAI 테스트 결과 (2026-01-24 오전)

```
입력: original.mkv (92.2분, 363.7MB)
출력: original_edited.mkv (68.9분, 272.5MB)
단축: 23.3분 (25.3%)
무음: 761개 감지
비용: 무료
시간: 1시간 27분
```

---

**문서 끝**
